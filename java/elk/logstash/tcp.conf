input {
  tcp {
    #codec => json
    port => '9563'
    #ssl_extra_chain_certs =>   # cacert
    # ssl_cert
    #ssl_enable
    # ssl_key
    # ssl_verify
  }
}

output {
  #file {
  #  path => "/var/log/goconserver/logstash.log"
    #codec => json_lines
  #}
  stdout { 
    #codec => json_lines
  }
}
